services:
  postgres:
    image: postgres
    container_name: postgresDB
    ports:
      - 5433:5432
    environment:
      POSTGRES_PASSWORD: 123
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: