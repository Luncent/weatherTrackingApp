services:

  database:
    image: postgres
    environment:
      POSTGRES_PASSWORD:${POSTGRES_PASSWORD}
    volumes:
      - postgres-data:/var/lib/postgresql/data

  app:
    image: weather-viewer
    build:
      dockerfile:

volumes:
  postgres-data: